# Tilahun Gessesse Audio Collection

This repository contains the permanent offline audio assets for the Tilahun Gessesse Mobile Application.

The tracks are stored as `.mp3` files named after their corresponding YouTube Video IDs (e.g., `f-kRyfLy-So.mp3`). This allows the mobile application to stream them reliably without facing YouTube anti-bot/rate-limiting blocks.
